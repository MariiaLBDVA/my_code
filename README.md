# my_code

Notebook exploring jarosite chemistry calculations and water-property interpolation.

Dependencies (install via `pip install pandas numpy matplotlib chemlib scipy plotly scikit-learn`) then open `code_notebook.ipynb` in Jupyter to reproduce the plots and tables.

Copilot for issue #4 is assigned to Codex.
